<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoquack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EleccionMenuActivity" />
        <activity android:name=".Activities.EleccionComidaActivity" />
        <activity android:name=".Activities.MenuActivity" />
        <activity android:name=".Activities.MenuJunaebActivity" />
        <activity android:name=".Activities.MenuNormalActivity" />

        <activity android:name=".Activities.ModificarPerfilActivity"></activity>
        <activity android:name=".Activities.NuevaComidaActivity"></activity>

        <activity android:name=".Activities.PerfilUsuarioActivity" />

        <activity
            android:name=".Activities.EstadoFilaActivity"
            android:label="@string/title_activity_estado_fila"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Activities.FilaActivity"
            android:label="@string/title_activity_fila" />
        <activity
            android:name=".Activities.FilaActivity2"
            android:label="@string/title_activity_fila" />

        <activity android:name=".Activities.RegistroActivity" />
        <activity android:name=".Activities.UsuarioActivity" />

        <activity android:name=".Activities.ValorarComentarActivity"></activity>


        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>